#include <stdio.h>

/**
 * main - Entry point
 * Description: 'from 0 to 9'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	};
	putchar('\n');
}
