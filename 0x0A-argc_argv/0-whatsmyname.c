#include <stdlib.h>
#include <stdio.h>

/**
 * main - PRINTS ITS NAME
 * @argc: no. of args
 * @argv: array of names
 * Return: 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	int arg_index = 0;

	printf("%s\n", argv[arg_index]);
	return (0);
}

