#include "main.h"
/**
 * more_numbers - loop in a loop
 *
 * Return: void
 */
void more_numbers(void)
{
	char a;
	char b;

	for (a = '0'; a <= '10'; a++)
	{
		for (b = '0'; b <= '14'; b++)
		{
			_putchar(b);
		};
		_putchar('\n');
	};
	_puthcar('\n')
}
